# BGMファイル配置場所

このディレクトリにBGMファイルを配置してください。

## ファイル名

- `stage.mp3` または `stage.ogg` - ステージBGM（Stage 1〜Bonus Stageまで再生）

## 対応フォーマット

- MP3 (推奨)
- OGG
- WAV

## 使用方法

1. BGMファイルをこのディレクトリに配置
2. ファイル名を `stage.mp3` にする
3. フロントエンドを起動するとStageScreen表示中に自動再生される
